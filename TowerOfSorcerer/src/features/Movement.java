package features;

import items.Creatures;

import java.awt.event.KeyEvent;

public class Movement {
	public void move(KeyEvent event, Creatures cha){
		if (event.getKeyCode() == KeyEvent.VK_UP) {
			
			}
	}
}
